<%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
        <div class="input-field col s4">
          <%= f.label :name %>
    <%= f.text_field :name %>
        </div>
        <div class="input-field col s4">
          <%= f.label :manufacturer %>
    <%= f.text_field :manufacturer %>
        </div>
        <div class="input-field col s4">
          <%= f.label :model %>
    <%= f.text_field :model %>
        </div>
      </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :length %>
    <%= f.text_field :length %>
  </div>

  <div class="field">
    <%= f.label :width %>
    <%= f.text_field :width %>
  </div>

  <div class="field">
    <%= f.label :height %>
    <%= f.text_field :height %>
  </div>

  <div class="field">
    <%= f.label :weight %>
    <%= f.text_field :weight %>
  </div>

  <div class="actions">
    
  </div>
<% end %>

<div class="row">
      <div class="col s1"><a href="<%= items_path %>"class="waves-effect waves-light btn amber">Cancel</a></div>
      <div class="col s9"></div>
      <div class="col s2 right-align"><button class="btn waves-effect waves-light green accent-3" type="submit" name="action">Submit
      <i class="material-icons right">send</i>
    </button></div>
    </div>